Use `binlog_search` once with the property name, then `binlog_properties` for the final value. If `binlog_evaluation_properties` is informative, call it once. Output a chronological assignment timeline (initial → reassignments → final) with source location for each. If the binlog has no initial-value events, suggest **Binlog: Rebuild with Property Tracking** (MsBuildLogPropertyTracking=15).
