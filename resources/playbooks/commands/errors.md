Use `binlog_lm_errors` (or `binlog_errors`) (and `binlog_warnings` if needed). Group by project. For each error report: error code, `file:line`, one-sentence cause, one-sentence fix. When multiple errors share a root cause (same missing reference, same generated-file issue, repeated across projects), say so explicitly and lead with the shared cause — fixing it once may resolve several errors. End with a one-line "Suggested fix order" if there are 3+ errors.
